# sort all files in increasing order of file size inside a folder
sudo du -sh * | sort -h

# becoming root user immediately
sudo -i

#clear screen and remove and remove all scroll-back
clear && printf '\e[3J'
